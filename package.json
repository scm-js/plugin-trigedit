{
  "name": "scmjs-plugin-trigedit",
  "version": "1.0.0",
  "private": true,
  "description": "TrigEdit — the Text Trigger Editor, a plugin for the scmJS StarCraft map editor.",
  "author": "Jeany",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "esbuild plugin.ts --bundle --format=esm --target=es2022 --platform=browser --outfile=dist/plugin.js --banner:js=\"// Generated by npm run build - do not edit; the source is plugin.ts.\"",
    "dev": "npm run build -- --watch",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@scm-js/plugin-api": "^1.18.0",
    "esbuild": "^0.28.2",
    "typescript": "~5.9.2",
    "vitest": "^3.2.4"
  }
}
